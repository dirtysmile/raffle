create table member (
    id int8 not null,
    username varchar(255),
    primary key (id)
);

CREATE TABLE account (
  id bigint not null,
  email varchar(255),
  test varchar(255),
  test2 varchar(255),
  test3 varchar(255)
)